import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bitcoin-tekus',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
